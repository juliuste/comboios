'use strict'

const stations = require('./stations')
const departures = require('./departures')
const trains = require('./trains')
const journeys = require('./journeys')

module.exports = { stations, departures, trains, journeys }
